# React-Advance
```
  1. Error Boundary [ComponentDidCatch || getDerivedStateFromError() ]

     ComponentDidCtch() - It is used to catch and handle errors that occur during the rendering of a component's child components.
     getDerivedStateFromError() - It is used to update the component state in response to an error occurring in any of its child components.

  2. Suspense
  3. React-Query
  4. Custom Hooks
  5. Memoization (useMemo, memo, useCallback)
  
```
